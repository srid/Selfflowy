name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - name: Build
        run: nix build

      - name: Smoke-check the built CLI
        run: ./result/bin/selfflowy check examples/Example.rkt Roadmap.rkt

      - name: Test
        run: nix develop -c just test
